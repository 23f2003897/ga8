# Question 1: Secret Hash Chain
In this task, I implemented a GitHub Action to process a unique secret and generate a verifiable SHA-256 hash.

### Learning Trace:
- Set up Repository Secret `MY_SECRET`.
- Created a YAML workflow to trigger on push.
- Used `sha256sum` to ensure data integrity.
